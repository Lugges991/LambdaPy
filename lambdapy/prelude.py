"""Prelude: standard definitions loaded by the REPL on start.

Currently empty — Nat, Vec, Fin, Eq are primitive InferTerm constructors
with hardcoded reduction rules in eval.py, so no prelude definitions are
needed for the core datatypes.
"""
from __future__ import annotations
